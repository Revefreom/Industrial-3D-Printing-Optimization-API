version: '3.8'

services:
  web-app:
    build: .
    container_name: 3d-printer-cost-app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
